- if current_user.following?(user)
    = link_to "Unfollow", admin_user_relationships_path(user.id), method: :delete
- else
    = link_to "Follow", admin_user_relationships_path(user.id), method: :post

