$(".EachData .inviteModal").html('<%= render partial: "inviteModal", locals: {users: @users, q: @q, group: @group} %>');

$("#inviteModal").on("shown.bs.modal", function() {
    $("inviteModal").find("input").focus();
});

$("#inviteModal").modal("show");